# MaatProof Python implementation

This folder contains the original Python reference implementation, including the `maatproof` package, Python project metadata, and pytest suite.

## Run checks

```powershell
Set-Location .\PYTHON
pip install -e ".[dev]"
python -m pytest tests -v
```

